type Erc20Transfer {
  id: ID!
  chainId: Int!
  blockNumber: BigInt!
  blockTimestamp: Int!
  transactionHash: String!
  transactionIndex: Int!
  logIndex: Int!
  tokenAddress: String!
  fromAddress: String!
  toAddress: String!
  valueRaw: BigInt!
}
