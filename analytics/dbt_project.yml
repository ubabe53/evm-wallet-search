name: evm_wallet_search
version: 0.1.0
config-version: 2
profile: evm_wallet_search

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]

vars:
  use_fixture: true

models:
  evm_wallet_search:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table

seeds:
  evm_wallet_search:
    +quote_columns: false
    raw_erc20_transfers_fixture:
      +column_types:
        id: varchar
        chain_id: integer
        block_number: bigint
        block_timestamp: bigint
        transaction_hash: varchar
        transaction_index: integer
        transaction_from_address: varchar
        transaction_to_address: varchar
        log_index: integer
        token_address: varchar
        from_address: varchar
        to_address: varchar
        value_raw: varchar
    token_rpc_metadata:
      +column_types:
        token_address: varchar
        name: varchar
        symbol: varchar
        decimals: integer
        rpc_block_number: bigint
        fetched_at: varchar
        fetch_status: varchar
        error_code: varchar
    token_rpc_metadata_fixture:
      +column_types:
        token_address: varchar
        name: varchar
        symbol: varchar
        decimals: integer
        rpc_block_number: bigint
        fetched_at: varchar
        fetch_status: varchar
        error_code: varchar
    counterparty_code_metadata:
      +column_types:
        chain_id: integer
        address: varchar
        account_type: varchar
        code_state: varchar
        code_size_bytes: bigint
        observation_block_number: bigint
        observation_block_timestamp: varchar
        eip7702_delegation_target: varchar
        safe_verified: boolean
        safe_verification_status: varchar
        safe_version: varchar
        safe_singleton_address: varchar
        safe_owner_count: integer
        safe_threshold: integer
        erc4337_observed: boolean
        erc4337_user_operation_count: bigint
        erc4337_first_observed_block: bigint
        erc4337_last_observed_block: bigint
        erc4337_entrypoint_address: varchar
        erc4337_entrypoint_version: varchar
        erc4337_entrypoint_source: varchar
        fetch_status: varchar
        reason_codes: varchar
        coverage_scope: varchar
        coverage_start_block: bigint
        coverage_end_block: bigint
        evidence_schema_version: varchar
        fetched_at: varchar
    counterparty_code_metadata_fixture:
      +column_types:
        chain_id: integer
        address: varchar
        account_type: varchar
        code_state: varchar
        code_size_bytes: bigint
        observation_block_number: bigint
        observation_block_timestamp: varchar
        eip7702_delegation_target: varchar
        safe_verified: boolean
        safe_verification_status: varchar
        safe_version: varchar
        safe_singleton_address: varchar
        safe_owner_count: integer
        safe_threshold: integer
        erc4337_observed: boolean
        erc4337_user_operation_count: bigint
        erc4337_first_observed_block: bigint
        erc4337_last_observed_block: bigint
        erc4337_entrypoint_address: varchar
        erc4337_entrypoint_version: varchar
        erc4337_entrypoint_source: varchar
        fetch_status: varchar
        reason_codes: varchar
        coverage_scope: varchar
        coverage_start_block: bigint
        coverage_end_block: bigint
        evidence_schema_version: varchar
        fetched_at: varchar
